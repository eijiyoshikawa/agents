# 11. 懸念点・論点リスト(伴走メモ)

このセッションで洗い出した「決めておきたい/詰めておきたい」論点。
新リポジトリ着手前に潰すほど手戻りが減る。★=特に早く決めたいもの。

---

## A. 業務・ドメイン

| # | 論点 | 仮の方針 | 確認先 |
|---|------|---------|--------|
| A1 ✅ | 締めは現場単位/得意先単位/会社単位のどれを正とするか | **決定:現場(配置先)単位を基本**。得意先で束ねて請求書発行。締め設定は配置先が保持(`12` ERD反映済) | — |
| A2 ✅ | 請求の「月極」と「スポット」の計算差 | **決定:両対応・現場ごとに `rate_form` で選択**。月極=契約月額固定/スポット=実績×単価(`05`§3-1, `12` Site) | — |
| A3 ✅ | 単価の「0=マスタ参照」をUIでどう置換するか | **決定:「マスタ参照/上書き」トグル**(`05`§1, `14` B, `12` rateOverrides) | — |
| A4 ✅ | キャンセル料(暴風雨中止等)の自動化範囲 | **決定:現場ごとにルール設定→半自動**。中止連絡日時から提案、確定は手動(`05`§3-2, `14` E②) | — |
| A5 ◐ | 協力会社(外注法人)の請求/支払の扱い | **仮決定:隊員として `flags=outsource` 登録。配置・実績は同様に扱い、支払(外注費)は給与とは別系統で管理可**。アカウント付与は任意 | 要確認(支払サイト/源泉) |
| A6 ✅ | 端数処理・外税/内税の現場ごとの実態 | **決定:現場(Site)設定で吸収**(taxMethod/rounding/taxRate、`12`) | — |
| A7 ◐ | 有給付与ロジック(4区分のどれを主に) | **仮決定:`月単位` と `所定労働日数8割判定` を主実装。`日数単位`/`対象外` も選択可**(隊員ごと `guard_paid_leave.unit`)。比例付与・繰越は実装時に労基準拠で詳細化 | 要確認(就業規則) |

## B. 日報・Drive 連携

| # | 論点 | 仮の方針 |
|---|------|---------|
| B1 ✅ | 紙原本の保管義務とDrive保存の関係(原本廃棄可否) | **決定:当面は原本も保管+Drive併用**(法務確認まで原本廃棄しない) |
| B2 | Drive保存先は自社Drive/共有ドライブどちらか | 会社設定で指定(`08`§3) |
| B3 | OCRをどこまで信頼するか | 補助のみ、人の承認必須 |
| B4 | 位置情報取得の同意・利用範囲 | 任意・同意ベース、到着確認補助のみ |
| B5 | 写真の保管年限・自動削除 | 法定年限ラベル+削除保護(要確定) |

## C. 権限・従業員アプリ

| # | 論点 | 仮の方針 |
|---|------|---------|
| C1 ✅ | 隊員アカウントの課金(無料枠の範囲) | **決定:一定人数まで無料+超過分のみ少額課金**(`06`/`09`§7に反映) |
| C2 | 単価・給与のマスキング粒度(誰に何を見せる) | ロール×項目マスク(`09`§3) |
| C3 | LINE連携を正式チャネルにするか | 補助導線として採用検討(個人情報保護要) |
| C4 | 退職者・外注の即時無効化運用 | 退社フラグ連動で即停止 |

## D. UI/UX・アクセシビリティ

| # | 論点 | 仮の方針 |
|---|------|---------|
| D1 ★ | 50代モニターでのユーザビリティテスト実施 | Phase1で必須化(`10`§6) |
| D2 | 内勤のキーボード操作(F1〜F12文化の踏襲度) | Web標準+主要ショートカット併設 |
| D3 | オフライン対応の実装範囲(日報のみ/打刻も) | まず日報・打刻を対象 |

## E. 技術・非機能

| # | 論点 | 仮の方針 |
|---|------|---------|
| E1 ✅ | マルチテナント設計(1社1テナント/支店分離) | **決定:テナント=会社、全テーブル `tenantId`+RLS で分離。支店はテナント内スコープ**(`12`) | — |
| E2 ◐ | 既存(JBCA)からのデータ移行要否 | **仮決定:乗り換え需要に備え CSV/Excel 取込ツールを用意(Phase後半)**。得意先/配置先/隊員/単価/残高を対象。JBCAも「コンバートシート(CSV)」方式(`15`)→同形式を取り込めると乗り換え障壁が下がる | 商談で判断 |
| E3 ◐ | 帳票PDFの生成方式・レイアウト確定 | **仮決定:サーバサイドPDF(テンプレート駆動)。法定様式(名簿5号・契約書)は実物に合わせ厳密レイアウト**。HTML→PDF系 or 帳票エンジンを選定 | 実装時選定 |
| E4 ◐ | 全銀フォーマット振込データの対応 | **仮決定:全銀協フォーマット(総合振込)を基本、主要行から。Phase2** | 取引銀行確認 |
| E5 ✅ | 監査ログ・データ保持・バックアップ要件 | **決定:AuditLog 標準装備(金額/単価/給与の変更・閲覧)。日次バックアップ・保持年限ラベル**(`12` AuditLog) | — |
| E6 ✅ | インボイス/軽減税率の境界ケース | **決定:登録番号・税率別記載を発行時必須チェック**(`04`/`14` E③) | — |

> 凡例:✅=決定 / ◐=仮決定(運用ヒアリングで微調整) / ★=未決の要確認

---

## F. カタログ(`15`)から新規に追加した論点

| # | 論点 | 仮の方針 |
|---|------|---------|
| F1 ★ | 電子サインの法的有効性(警備日報・契約) | **仮決定:電子サイン+紙写真の両対応**。契約系は要件確認(電子帳簿保存法・取引先合意) |
| F2 | 資格者配置 路線検索のデータ源 | 隊員保有資格 × 現場要求資格 × 路線。路線マスタの要否は運用ヒアリング |
| F3 | 取引先への日報控え自動送信の同意・宛先 | 配置先の送付先アドレスへ。送信可否は得意先設定 |
| F4 | AI分析/自然言語クエリを内蔵するか(差別化) | **方針:標準内蔵を志向**(Phase4)。LLM選定・コスト・データ境界は別途設計 |
| F5 | 教育Pro相当(備付書類)の実装時期 | Phase4。痛点83.3%だが基幹優先のため後段 |

---

## 現時点の残・要確認(運用ヒアリング推奨)

データモデルは確定方針で組めるが、以下は**現場の運用実態**を聞いて精緻化したい(仮決定で先行可):

- **A5 外注の支払系統**:外注費の支払サイト・源泉・インボイス受領の扱い。
- **A7 有給の細則**:比例付与・繰越・時間単位年休の要否(就業規則準拠)。
- **B5 写真保管年限**:契約・名簿・賃金台帳の法定保存年限に合わせた自動アーカイブ/削除。
- **C3 LINE連携**:正式チャネル化するか(個人情報・なりすまし対策の設計が必要)。
- **D1 ユーザビリティテスト**:50代モニターの確保(Phase1ゲート)。

→ これらは「仮決定」で実装を止めずに進められる。確定し次第このファイルを更新する。
